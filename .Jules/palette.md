## 2025-05-14 - Skip Link and ARIA-pressed states for Static Landings
**Learning:** Adding a "Skip to Content" link and using 'aria-pressed' for selection buttons significantly improves keyboard and screen reader navigation in static landing pages where navigation headers can be dense.
**Action:** Always check for a skip link in the initial HTML and ensure toggle-like buttons communicate their state semantically via ARIA attributes.
